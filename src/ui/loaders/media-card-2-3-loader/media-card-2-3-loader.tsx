import { FC } from 'react';
import styles from './media-card-2-3-loader.module.scss';

export const MediaCard23Loader: FC = () => {
    return (
        <div className={styles.card}>
            <div className={styles.infoPanel} />
        </div>
    );
};
